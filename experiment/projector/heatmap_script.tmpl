        const xAxisData = {{.XAxisDataJSON}};
        const yAxisData = {{.YAxisDataJSON}};
        const heatmapData = {{.DataJSON}};
        const minVal = {{.Min}};
        const maxVal = {{.Max}};
        const chart = echarts.init(document.getElementById('chart'), null, { renderer: 'svg' });
        
        const option = {
            backgroundColor: 'transparent',
            animation: false,
            tooltip: { position: 'top' },
            grid: { left: '10%', right: '15%', top: '10%', bottom: '15%' },
            xAxis: {
                type: 'category',
                data: xAxisData,
                axisLabel: { color: '#475569' }
            },
            yAxis: {
                type: 'category',
                data: yAxisData,
                axisLabel: { color: '#475569' }
            },
            visualMap: {
                min: minVal,
                max: maxVal,
                calculable: true,
                orient: 'vertical',
                right: '5%',
                top: 'center',
                inRange: {
                    color: ['#1e3a5f','#2563eb','#60a5fa','#e8f0fe','#fef3c7','#f59e0b','#c2410c','#7c2d12']
                }
            },
            series: [{
                name: 'Heatmap',
                type: 'heatmap',
                data: heatmapData,
                itemStyle: { borderWidth: 0 }
            }]
        };
        chart.setOption(option);
