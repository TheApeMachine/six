\subsection{Eigenmode Role Phases}
We computed eigenphases from symbol transition matrices and aggregated span roles.
{{len .Roles}} structural roles (header, loop, conditional, return, etc.) showed distinct phase centroids,
with {{.WellSepCount}} of {{.TotalPairs}} role pairs well-separated (distance/std ratio \(>1\)).
