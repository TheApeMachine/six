\subsection{Span Chaining}
Multi-span chaining over the Python corpus produced {{.ReturnCount}} functions with \texttt{return} and
{{.LoopCount}} with loops. Chain length capped at {{.MaxChains}} steps per prompt.
