\subsection{Eigenmode Role Phases}
We computed eigenphases from symbol transition matrices and aggregated span roles.
6 structural roles (header, loop, conditional, return, etc.) showed distinct phase centroids,
with 3 of 15 role pairs well-separated (distance/std ratio \(>1\)).
