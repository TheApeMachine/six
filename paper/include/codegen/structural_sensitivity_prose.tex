\subsection{Structural Sensitivity}
We probed whether the fingerprint distinguishes correct continuations from comment and noise variants.
For each of 4 functions, \texttt{sim(correct, full)} exceeded both \texttt{sim(comment, full)}
and \texttt{sim(noise, full)}, confirming structural sensitivity of the encoding.
