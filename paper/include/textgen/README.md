# Textgen paper data

`compositional_results.json` (and related JSON under this folder) intentionally use an empty `"results": []` array when no benchmark rows are embedded in the paper build. Regenerate or paste fresh runs when updating the manuscript; historical blobs are not kept in-tree.
