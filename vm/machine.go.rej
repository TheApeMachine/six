@@ -190,6 +190,14 @@
 			if missingChord.ActiveCount() == 0 {
 				startIndex = bestIdx + len(prompt)
 			}
+fmt.Printf("\nDEBUG BestIdx string at %d:\n", bestIdx)
+for offset := -10; offset < 20; offset++ {
+if bestIdx+offset >= 0 && bestIdx+offset < machine.primefield.N {
+rawChord := machine.primefield.Raw(bestIdx + offset)
+fmt.Printf("%c", tokenizer.NewMortonCoder().Decode(rawChord.Indices()[0]))
+}
+}
+fmt.Printf("\n")
 			for offset := 0; offset < 10 && startIndex+offset < machine.primefield.N; offset++ {
 				rawChord := machine.primefield.Raw(startIndex + offset)
 				var singleChord data.MultiChord
